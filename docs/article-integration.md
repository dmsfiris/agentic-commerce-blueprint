# Article integration

Suggested article wording:

> A public reference implementation accompanies this article. It includes the canonical decision-envelope v4 contract, action-aware decision basis, detached authenticators, generated-claim capability gates, evidence hash-pinning, freshness and dependency handling, public/MCP-style/operator projections, JSON Schema, runnable reference scenarios, generated example outputs, and focused semantic checks.

The repository tree is concrete so readers can inspect and run the model. The broader production ownership map in the article remains conceptual; a production commerce platform does not need to copy this directory structure literally.
